#include "bfstructoperate.h"
//bool isNeedExcuteHide(QVector<QSharedPointer<const IFolderInfo>>vec_folder_info) {
//    //问文件夹右键菜单拿取文件夹组
//    for (int i = 0; i < vec_folder_info.size(); ++i) {
//      auto one_item = vec_folder_info.at(i);
//      if (one_item && !one_item->isHidden()) {
//        //【隐藏】
//        return true;
//      }
//    }
// 
//  return false;
//}